#ifndef SETTINGS_H
#define SETTINGS_H

const char* SSL_FINGERPRINT = "";  // Torn SSL cert's SHA1 print
const char* API_KEY = "";          // Torn API key

const char* WIFI_SSID = "";        // WiFi SSID
const char* WIFI_PASSWORD = "";    // WiFi password

#endif
